# Transparent Ops Governance Frontend

## Getting Started

Install yarn if not already installed:
```shell
$ npm install -g yarn
```

Install the dependencies:

```shell
$ yarn install
```

Next, run the development server:

```shell
$ yarn dev
```

Running the build:
```shell
$ yarn build
$ yarn start
```
